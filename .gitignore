# Hugo build output
/public/
/public-test/
/resources/
/hugo_stats.json

# OS / IDE files
.DS_Store
*.log

# Private data
*.pem
.env
.env.local

# Private content (암호화 전 원본)
content/private/

# 암호화된 콘텐츠는 Git에 포함 (배포용)
# static/private-encrypted/


# DDNS 관련 파일 (API Token 포함)
update-cloudflare-ip.sh
ddns.log
*.env*
blog-k8s-project/was/

# 내부 문서 정리 완료 (2026-01-20)
# docs/ 디렉터리를 Git에서 추적하여 문서 이력 관리
# 민감 정보가 있는 경우 개별 파일로 제외

# Istio 바이너리
istio-*/
