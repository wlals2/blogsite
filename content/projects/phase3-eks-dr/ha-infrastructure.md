---
title: "견고한 인프라 설계: AZ 장애에도 버티는 고가용성 아키텍처"
date: 2026-01-13
summary: "Canary 배포 도입 중 발견한 세션 문제에서 시작해, Multi-AZ 고가용성까지 구축한 여정"
tags: ["eks", "kubernetes", "ha", "multi-az", "redis", "session", "canary"]
categories: ["projects"]
series: ["Infrastructure Learning Journey"]
weight: 3
showtoc: true
tocopen: true
---

# 견고한 인프라 설계: AZ 장애에도 버티는 고가용성 아키텍처

> Canary 배포 도입 중 발견한 세션 문제에서 시작해, Multi-AZ 고가용성까지 구축한 여정을 정리했습니다.

---

## 📖 문제의 시작: Canary 배포가 세션을 깨뜨렸다

Rolling Update에서 Canary 배포로 전환하기로 했습니다. 신규 버전에 버그가 있어도 10% 트래픽만 영향받도록 하기 위해서였죠.

그런데 Canary 배포를 적용하자마자 예상치 못한 문제가 발생했습니다.

### 증상: 사용자 로그인이 자꾸 풀린다

Canary 배포가 진행되면서, Stable Pod(90%)와 Canary Pod(10%)로 트래픽이 나뉘었습니다. 문제는 사용자가 Stable Pod에서 로그인한 후, 다음 요청이 Canary Pod로 가면 **세션이 없어서 로그인이 풀리는 것**이었습니다.

```
사용자 → Stable Pod → 로그인 → 세션 저장 (Stable Pod 메모리)
     ↓
사용자 → Canary Pod → 세션 없음 → 로그인 풀림!
```

각 Pod가 세션을 자기 메모리에만 저장하고 있었기 때문입니다.

### 해결: Redis Session Clustering

세션을 Redis에 중앙 집중 저장하도록 변경했습니다.

**Spring Boot 설정:**
```yaml
# application.yml
spring:
  session:
    store-type: redis
  redis:
    host: redis-master.petclinic.svc.cluster.local
    port: 6379
```

**Redis Session Config:**
```java
@EnableRedisHttpSession(maxInactiveIntervalInSeconds = 1800)
public class RedisSessionConfig {
    @Bean
    public RedisSerializer<Object> springSessionDefaultRedisSerializer() {
        return new GenericJackson2JsonRedisSerializer();
    }
}
```

이제 어느 Pod로 요청이 가든 Redis에서 세션을 조회하므로 로그인 상태가 유지됩니다.

**성과:**
- 세션 유지율: 50% → **100%**
- WAS replica: 1개 고정 → **2-10개** HPA 스케일링 가능

**문서 링크:**
- [Redis Session Clustering 상세 구현](https://github.com/wlals2/bespin-project/blob/main/docs/session/001-redis-session-clustering-implementation.md)

---

## 🚨 더 큰 문제 발견: 모든 Pod가 한 AZ에 몰려있다

세션 문제를 해결하고 나니, 또 다른 고민이 생겼습니다.

> **"만약 AZ 자체가 죽으면 어떻게 되지?"**
>
> AWS AZ 장애는 실제로 발생한 적 있습니다. (2019년 도쿄, 2020년 서울)

Pod 분산 상태를 확인해보기로 했습니다.

### Pod 분산 상태 확인

```bash
kubectl get pods -n petclinic -o wide --no-headers | \
  awk '{print $1, $7}' | \
  while read pod node; do
    zone=$(kubectl get node $node -o jsonpath='{.metadata.labels.topology\.kubernetes\.io/zone}')
    echo "$pod | $node | $zone"
  done
```

결과를 보고 충격받았습니다.

```
┌─────────────────────┬─────────────────────┬─────────────────┐
│ Pod                 │ Node                │ AZ              │
├─────────────────────┼─────────────────────┼─────────────────┤
│ was-xxx-1           │ ip-10-0-12-217      │ ap-northeast-2c │
│ was-xxx-2           │ ip-10-0-12-89       │ ap-northeast-2c │
│ web-xxx-1           │ ip-10-0-12-217      │ ap-northeast-2c │
│ web-xxx-2           │ ip-10-0-12-89       │ ap-northeast-2c │
│ redis-master-0      │ ip-10-0-12-217      │ ap-northeast-2c │
└─────────────────────┴─────────────────────┴─────────────────┘
```

**모든 Pod가 ap-northeast-2c 한 곳에 몰려있었습니다.** 2c AZ에 장애가 나면 전체 서비스가 중단됩니다.

### 왜 이렇게 됐는가?

Kubernetes 스케줄러는 기본적으로 **리소스가 충분한 노드**를 우선 선택합니다. AZ 분산은 고려하지 않죠. 2c AZ 노드들이 리소스 여유가 더 많았기 때문에 자연스럽게 Pod가 몰린 것입니다.

```
Before: 모든 Pod가 한 AZ에 집중
────────────────────────────────────────────────────

   AZ: ap-northeast-2a          │    AZ: ap-northeast-2c
  ┌─────────────────────┐       │   ┌─────────────────────┐
  │                     │       │   │ ┌─────┐ ┌─────┐     │
  │  (텅 빔)            │       │   │ │WAS 1│ │WAS 2│     │
  │                     │       │   │ └─────┘ └─────┘     │
  │                     │       │   │ ┌─────┐ ┌─────┐     │
  │                     │       │   │ │WEB 1│ │WEB 2│     │
  │                     │       │   │ └─────┘ └─────┘     │
  │                     │       │   │ ┌───────────┐       │
  │                     │       │   │ │   Redis   │       │
  └─────────────────────┘       │   └─────────────────────┘

리스크: 2c AZ 장애 시 100% 서비스 중단
```

---

## 🛠️ 해결: topologySpreadConstraints로 Pod 강제 분산

### 왜 topologySpreadConstraints인가?

여러 대안을 비교했습니다.

| 방법 | 장점 | 단점 |
|------|------|------|
| Pod Anti-Affinity | 간단함 | 노드 레벨만, AZ 분산 보장 안 됨 |
| **topologySpreadConstraints** | AZ/노드 레벨 모두 강제 분산 | 설정이 조금 복잡 |
| Karpenter 분산 정책 | 노드 생성 시 자동 분산 | 기존 Pod는 해결 안 됨 |

**topologySpreadConstraints를 선택한 이유:**
- `maxSkew: 1`로 AZ 간 Pod 수 차이를 1개 이내로 제한
- `whenUnsatisfiable: DoNotSchedule`로 분산 불가능하면 스케줄링 거부 (안전성 우선)

### 적용한 설정

WAS Rollout에 다음 설정을 추가했습니다.

```yaml
spec:
  template:
    spec:
      topologySpreadConstraints:
        # AZ 레벨 분산 (필수)
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              app: petclinic
              tier: was
        # 노드 레벨 분산 (권장)
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app: petclinic
              tier: was
```

**설정 값 선택 이유:**

| 설정 | 값 | 이유 |
|------|-----|------|
| `maxSkew` | 1 | AZ 간 Pod 수 차이를 1개 이내로 제한 |
| `topologyKey` | zone | AZ 레벨 분산 (노드 장애보다 AZ 장애가 더 치명적) |
| `whenUnsatisfiable` | DoNotSchedule | 분산 불가능하면 스케줄링 거부 (안전성 우선) |

### 적용 방법

```bash
cd ~/CICD/manifestrepo
git add was/rollout.yaml web/deployment.yaml
git commit -m "Add topologySpreadConstraints for Multi-AZ HA"
git push origin main

# ArgoCD 자동 Sync 후 Pod 재시작
kubectl argo rollouts restart was -n petclinic
kubectl rollout restart deployment web -n petclinic
```

---

## 🎉 결과: Multi-AZ 분산 달성

적용 후 Pod 분산 상태를 다시 확인했습니다.

```
┌─────────────────────┬─────────────────────┬─────────────────┐
│ Pod                 │ Node                │ AZ              │
├─────────────────────┼─────────────────────┼─────────────────┤
│ was-xxx-1           │ ip-10-0-11-47       │ ap-northeast-2a │
│ was-xxx-2           │ ip-10-0-12-217      │ ap-northeast-2c │
│ web-xxx-1           │ ip-10-0-11-71       │ ap-northeast-2a │
│ web-xxx-2           │ ip-10-0-12-89       │ ap-northeast-2c │
│ redis-master-0      │ ip-10-0-12-217      │ ap-northeast-2c │
└─────────────────────┴─────────────────────┴─────────────────┘
```

WAS, WEB Pod가 2a, 2c AZ에 균등 분산되었습니다.

```
After: Multi-AZ 분산 완료
────────────────────────────────────────────────────

   AZ: ap-northeast-2a          │    AZ: ap-northeast-2c
  ┌─────────────────────┐       │   ┌─────────────────────┐
  │ ┌─────┐             │       │   │ ┌─────┐             │
  │ │WAS 1│             │       │   │ │WAS 2│             │
  │ └─────┘             │       │   │ └─────┘             │
  │ ┌─────┐             │       │   │ ┌─────┐             │
  │ │WEB 1│             │       │   │ │WEB 2│             │
  │ └─────┘             │       │   │ └─────┘             │
  │                     │       │   │ ┌───────────┐       │
  │                     │       │   │ │   Redis   │       │
  └─────────────────────┘       │   └─────────────────────┘

한 AZ 장애 시에도 50% 서비스 유지
```

---

## 📊 성과 요약

| 지표 | Before | After | 개선 |
|------|--------|-------|------|
| Pod 분산 | 한 AZ에 100% 몰림 | Multi-AZ 50/50 분산 | 균등 분산 |
| AZ 장애 시 가용성 | 0% (전체 중단) | 50% (절반 유지) | 서비스 연속성 확보 |
| 예상 SLA | 95% | 99.9% | +4.9% |
| 세션 유지율 | 50% | 100% | 2배 |
| WAS 스케일 | 1 replica 고정 | 2-10 replica HPA | 10배 |

### 남은 과제

Redis가 2c AZ에만 있어 SPOF(단일 장애점)입니다. Redis Sentinel 또는 ElastiCache for Redis (Multi-AZ) 도입을 검토 중입니다.

---

## 💡 핵심 교훈

### 1. Canary 배포 도입 시 세션 공유는 필수

Pod 간 트래픽 분산이 일어나면 세션 문제가 바로 발생합니다. Stateless 아키텍처를 위해 Redis Session Clustering은 필수입니다.

### 2. Kubernetes 스케줄러는 AZ 분산을 고려하지 않는다

명시적으로 `topologySpreadConstraints` 설정이 필요합니다. 기본 스케줄러는 리소스 여유만 보고 스케줄링합니다.

### 3. 문제 해결 후 "더 큰 장애 시나리오"를 고민하라

세션 문제 해결 → "AZ 장애는?" → Multi-AZ 분산으로 자연스럽게 이어졌습니다. 한 단계 더 나아가 생각하는 습관이 중요합니다.

---

## 📚 관련 문서

- [Kubernetes: Pod Topology Spread Constraints](https://kubernetes.io/docs/concepts/scheduling-eviction/topology-spread-constraints/)
- [Kubernetes: Affinity and Anti-Affinity](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/)
- [Redis Session Clustering 구현 가이드](https://github.com/wlals2/bespin-project/blob/main/docs/session/001-redis-session-clustering-implementation.md)
- [Multi-AZ 고가용성 아키텍처 가이드](https://github.com/wlals2/bespin-project/blob/main/docs/architecture/multi-az-ha-guide.md)

---

**다음 읽기:**
- [Canary 배포 전략과 트래픽 라우팅](./canary-deployment.md)
- [DR (Disaster Recovery) 아키텍처 설계](./dr-architecture.md)
