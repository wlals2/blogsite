# Hugo 블로그 HTTPS 트러블슈팅 과정

## 문제 증상
"Hugo 블로그가 갑자기 HTTPS로 접속이 안 됨"

---

## 🔍 트러블슈팅 단계별 분석

### 1단계: 환경 확인 및 기본 정보 수집

#### 수행한 명령어
```bash
# Hugo 설치 확인 및 버전 체크
hugo version

# 블로그 디렉토리 구조 확인
ls -la /home/jimin/blogsite/

# 설정 파일 찾기
find ~ -name "config.toml" -o -name "config.yaml"
```

#### 발견한 사실
- Hugo v0.146.0 설치됨 ✅
- 블로그 위치: `/home/jimin/blogsite/` ✅
- 설정 파일: `config.toml` 존재 ✅

#### 왜 이 작업이 필요한가?
- 문제를 해결하기 전에 "무엇이 있는지" 파악
- Hugo가 정상 설치되어 있는지 확인
- 프로젝트 구조 이해

---

### 2단계: Hugo 빌드 테스트

#### 수행한 명령어
```bash
cd /home/jimin/blogsite
hugo
```

#### 결과
```
Total in 453 ms
Pages: 279
✅ 빌드 성공
```

#### 발견한 사실
- Hugo 빌드 자체는 정상 작동 ✅
- public/ 폴더에 정적 파일 생성됨
- **문제는 Hugo가 아니라 서버 설정**

#### 핵심 인사이트
빌드가 성공했다는 것은 **코드/콘텐츠 문제가 아님**을 의미합니다.
→ 문제는 **배포/네트워크/서버** 쪽에 있을 가능성 높음

---

### 3단계: 웹 서버 확인

#### 수행한 명령어
```bash
# 실행 중인 웹 서버 프로세스 확인
ps aux | grep -E "hugo|nginx|apache|caddy"

# 설치된 웹 서버 확인
which nginx caddy apache2
```

#### 결과
```
nginx: master process 실행 중 ✅
12개의 worker process 실행 중
```

#### 발견한 사실
- Nginx가 실행 중 ✅
- Hugo server가 아닌 **프로덕션 환경** (Nginx)
- **문제는 Nginx 설정이나 연결**

#### 왜 중요한가?
- Hugo server (개발용)와 Nginx (프로덕션)는 다른 방식
- 각각 다른 트러블슈팅 접근이 필요

---

### 4단계: HTTPS 접속 테스트

#### 수행한 명령어
```bash
# 외부에서 HTTPS 접속 테스트
curl -I https://blog.jiminhome.shop
```

#### 결과
```
HTTP/2 522
server: cloudflare
```

#### 발견한 핵심 문제!
**HTTP 522 에러** = Cloudflare가 오리진 서버에 연결 실패

#### 에러 코드 해석
- **522 Error**: Connection timed out
- Cloudflare ↔ Origin Server 연결 문제
- 서버가 꺼져있거나, 방화벽 차단, 또는 **IP 주소 불일치**

---

### 5단계: 로컬 서버 테스트 (중요!)

#### 수행한 명령어
```bash
# 로컬에서 직접 서버 접속 테스트
curl -I http://localhost

# SSL 검증 무시하고 HTTPS 테스트
curl -I -k https://localhost
```

#### 결과
```
HTTP/2 200 ✅
server: nginx/1.18.0
```

#### 핵심 발견!
- 서버는 **정상 작동 중** ✅
- Nginx 설정도 정상 ✅
- 문제는 **외부 연결** (Cloudflare → 서버)

#### 논리적 추론
```
✅ Hugo 빌드 성공
✅ Nginx 실행 중
✅ 로컬 접속 성공
❌ Cloudflare → 서버 실패
→ 결론: DNS나 IP 주소 문제!
```

---

### 6단계: 네트워크 진단

#### 수행한 명령어
```bash
# 서버의 실제 공인 IP 확인
curl ifconfig.me

# DNS가 가리키는 IP 확인
dig +short blog.jiminhome.shop

# 더 자세한 DNS 조회
nslookup blog.jiminhome.shop
```

#### 결과
```
실제 서버 IP: 122.46.102.106
DNS가 가리키는 IP: 122.46.102.248  ❌❌❌
```

#### 🎯 문제 발견!
**DNS IP ≠ 실제 서버 IP**

이것이 522 에러의 원인!

---

### 7단계: 실제 IP로 직접 접속 테스트 (검증)

#### 수행한 명령어
```bash
# 실제 서버 IP로 직접 HTTPS 접속
curl -I -k https://122.46.102.106 -H "Host: blog.jiminhome.shop"
```

#### 결과
```
HTTP/2 200 ✅
```

#### 검증 완료!
실제 IP로 직접 연결하면 작동함
→ **DNS IP 주소가 잘못되었음을 확인**

---

## 🎯 문제 원인 분석

### 왜 이런 일이 발생했나?

1. **동적 IP 환경**
   - ISP가 동적 IP 할당
   - 이전 IP: 122.46.102.248
   - 현재 IP: 122.46.102.106
   - IP가 변경되었지만 DNS는 업데이트 안 됨

2. **Cloudflare DNS 업데이트 안 됨**
   - A 레코드가 옛날 IP를 가리킴
   - Cloudflare가 존재하지 않는 서버에 연결 시도
   - 522 에러 발생

---

## 💡 해결 방법

### 임시 해결 (수동)
1. Cloudflare 대시보드 접속
2. DNS → A 레코드 편집
3. IP 주소를 현재 IP로 변경

### 영구 해결 (자동화)
**DDNS (Dynamic DNS) 스크립트 작성**

#### 스크립트 작동 원리
```bash
1. 현재 공인 IP 확인 (curl ifconfig.me)
2. Cloudflare DNS에 설정된 IP 확인 (API 호출)
3. IP가 다르면 자동 업데이트
4. Cron으로 5분마다 자동 실행
```

---

## 🔧 사용한 핵심 명령어 정리

### 1. 서비스 상태 확인
```bash
ps aux | grep nginx          # 프로세스 확인
systemctl status nginx       # 서비스 상태
nginx -t                     # 설정 파일 검증
```

### 2. 네트워크 진단
```bash
curl ifconfig.me            # 공인 IP 확인
nslookup domain.com         # DNS 조회
dig domain.com              # 상세 DNS 조회
dig +trace domain.com       # DNS 위임 추적
netstat -tlnp               # 리스닝 포트 확인
```

### 3. HTTP/HTTPS 테스트
```bash
curl -I http://domain.com           # HTTP 헤더 확인
curl -I https://domain.com          # HTTPS 헤더 확인
curl -I -k https://domain.com       # SSL 검증 무시
curl -I https://IP -H "Host: domain" # 특정 IP로 접속
```

### 4. DNS 분석
```bash
nslookup domain.com 8.8.8.8   # 특정 DNS 서버로 조회
dig domain.com +short          # 간단한 결과만
dig domain.com +trace          # 전체 위임 과정
```

---

## 📊 트러블슈팅 플로우차트

```
블로그 접속 안 됨
    ↓
Hugo 빌드 테스트
    ↓
    ├─ 실패 → Hugo 설정/코드 문제
    ↓
    └─ 성공 → 서버/네트워크 문제
        ↓
    웹 서버 확인 (nginx/apache)
        ↓
        ├─ 실행 안 됨 → 서버 시작
        ↓
        └─ 실행 중 → 연결 문제
            ↓
        로컬 접속 테스트
            ↓
            ├─ 실패 → Nginx 설정 문제
            ↓
            └─ 성공 → 외부 연결 문제
                ↓
            외부 접속 테스트
                ↓
                ├─ 522 에러 → DNS/IP 문제 ✅
                ├─ 403/404 → 웹 서버 설정
                ├─ 502/503 → 백엔드 문제
                └─ 타임아웃 → 방화벽/포트
                    ↓
                DNS vs 실제 IP 비교
                    ↓
                    ├─ 일치 → 방화벽 확인
                    ↓
                    └─ 불일치 → DNS 업데이트 ✅
```

---

## 🎓 트러블슈팅 핵심 원칙

### 1. 레이어별 접근 (Layer by Layer)
```
Application (Hugo) ✅
    ↓
Web Server (Nginx) ✅
    ↓
Network (Local) ✅
    ↓
Network (External) ❌
    ↓
DNS ❌ ← 여기서 문제 발견!
```

### 2. 작동하는 것과 안 되는 것 구분
- ✅ 로컬 접속 작동
- ❌ 외부 접속 안 됨
- → **로컬과 외부의 차이점**이 문제

### 3. 에러 코드 이해
- 522: Cloudflare ↔ 서버 연결 실패
- 이것만 알아도 문제 범위가 좁혀짐

### 4. 로그와 도구 활용
```bash
# 에러 로그
tail -f /var/log/nginx/error.log

# 액세스 로그
tail -f /var/log/nginx/access.log

# 시스템 로그
journalctl -xe -u nginx
```

### 5. 가설 수립과 검증
```
가설 1: Hugo 문제? → 빌드 성공 → 기각
가설 2: Nginx 문제? → 로컬 접속 성공 → 기각
가설 3: 방화벽 문제? → 포트는 열림 → 기각
가설 4: DNS 문제? → IP 불일치 발견 → 채택! ✅
```

---

## 🛠️ 추가 진단 도구

### Cloudflare 특정 문제
```bash
# Cloudflare IP 확인
dig blog.jiminhome.shop
# 104.x.x.x 또는 172.x.x.x → Cloudflare 프록시 사용 중

# 실제 오리진 IP 확인
dig blog.jiminhome.shop +trace
```

### SSL 인증서 확인
```bash
# 인증서 정보 확인
openssl s_client -connect blog.jiminhome.shop:443 -servername blog.jiminhome.shop

# 만료일 확인
echo | openssl s_client -connect blog.jiminhome.shop:443 2>/dev/null | openssl x509 -noout -dates
```

### 방화벽 확인
```bash
# UFW
sudo ufw status

# iptables
sudo iptables -L -n

# 포트 리스닝 확인
sudo netstat -tlnp | grep :443
```

---

## 📝 배운 교훈

### 1. 에러 메시지를 정확히 해석하기
- HTTP 522 = Cloudflare specific error
- 구글에서 "Cloudflare 522 error" 검색하면 원인 파악 가능

### 2. 단계별 확인의 중요성
- 한 번에 모든 것을 의심하지 말 것
- 레이어별로 하나씩 검증

### 3. 동적 IP 환경에서는 DDNS 필수
- 수동으로 IP 업데이트는 비효율적
- 자동화가 답

### 4. 문서화의 중요성
- 트러블슈팅 과정 기록
- 다음에 같은 문제 발생 시 빠른 해결

---

## 🚀 예방 조치

### 1. 모니터링 설정
```bash
# 웹사이트 상태 확인 스크립트
#!/bin/bash
if ! curl -f -s https://blog.jiminhome.shop > /dev/null; then
    echo "블로그 다운!" | mail -s "Alert" your@email.com
fi
```

### 2. 정기 백업
```bash
# Nginx 설정 백업
sudo cp /etc/nginx/sites-available/blog /etc/nginx/sites-available/blog.backup

# Hugo 콘텐츠 Git 커밋
cd /home/jimin/blogsite && git add . && git commit -m "backup"
```

### 3. 알림 설정
- UptimeRobot 같은 서비스로 다운타임 모니터링
- Cloudflare Email Alerts 활성화

---

## 📚 참고 자료

### HTTP 상태 코드
- 200: 성공
- 301/302: 리다이렉트
- 403: 권한 없음
- 404: 페이지 없음
- 500: 서버 내부 오류
- 502: Bad Gateway (백엔드 연결 실패)
- 503: Service Unavailable
- 522: Cloudflare - Origin Connection Timeout

### Cloudflare 에러
- 520: Unknown Error
- 521: Web Server Is Down
- 522: Connection Timed Out ← 오늘 문제
- 523: Origin Is Unreachable
- 524: A Timeout Occurred

### DNS TTL
- TTL이 길면 DNS 변경이 느리게 전파됨
- DDNS 사용 시 TTL=300 (5분) 권장

---

## 🎯 요약: 3단계 진단법

### 1단계: 로컬 테스트
```bash
curl -I http://localhost
# 성공? → 서버는 정상, 외부 연결 문제
# 실패? → 서버 자체 문제
```

### 2단계: DNS 확인
```bash
nslookup your-domain.com
curl ifconfig.me
# IP 일치? → 방화벽/프록시 문제
# IP 불일치? → DNS 업데이트 필요
```

### 3단계: 직접 IP 테스트
```bash
curl -I https://실제-IP -H "Host: your-domain.com"
# 성공? → DNS 문제 확정
# 실패? → 서버 설정 문제
```

---

이런 체계적인 접근으로 문제를 정확히 찾아낼 수 있었습니다! 🎉
